{
  "framework": "vite",
  "buildCommand": "cd music-magnate && npm install && npm run build",
  "outputDirectory": "music-magnate/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}